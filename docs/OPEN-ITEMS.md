# 未完成工作任务清单

| | |
|---|---|
| 生成日期 | 2026-09-09 |
| 分支 | `feat/p1-foundation`（16 提交，CI 绿，**未合入 main**） |
| 依据 | WBS · [评审裁决报告](REVIEW-PANEL-2026-09-03.md) · [P1 交付评审](P1-DELIVERY-REVIEW.md) · 本次逐项核实 |

> 本清单只列**尚未完成**的事。已接受的风险单列在 §5，那不是任务，是不必重复讨论的决定。
> 人天估算沿用 WBS 原值，未作调整。

---

## 1. P1 残留（不阻塞合入，但属 P1 范围）

| # | 任务 | 为什么还得做 | 触发时机 |
|---|---|---|---|
| 1.1 | **悬挂 attempt 检测器** | 「attempt 行可检测出来」是两段式写入的**全部理由**。实测 60 条写操作 0 悬挂 —— 机制正常，但缺检测手段，等于性质无背书 | 合入前后皆可 |
| 1.2 | **`response_payload` / `evidence_ref` 两列无写入路径** | 建了列却无人写。涉及脱敏（宪法第八条），**现在决定比 P4 补便宜**。`evidence_ref` 关联第九条 | P2 开工前定 |
| 1.3 | **恢复演练只覆盖 `agentsystem`** | `backup.sh` 备了 `dify.dump` 与 `dify-storage.tar.gz`，`restore_drill.sh` 一处未提。且 storage 缺失时是**静默跳过**，不影响退出码 | P3 灌语料前（3 人收敛） |
| 1.4 | **`main` 分支保护规则未启用** | 远程已就绪（`PunjabLee/AgentSystem`），"禁止直接推送 + CI 必过"尚未开。当前靠约定 | 合入 main 时 |

## 2. P2 准入前置（开工前或开工当天）

| # | 任务 | 为什么必须前置 | 触发时机 |
|---|---|---|---|
| 2.1 | **连接池真实负载验证** | 共池死锁那次用的是**裸引擎**，非 FastAPI 请求路径。业务池 5 / 审计池 3 且 `max_overflow=0`，P2.3.1 落地即是这两个数字第一次受真实检验。另：`_make_engine` 未设 `pool_timeout`，默认 30 秒静默排队，易与 LLM 高延迟混淆 | **P2.3.1 落地当天第一件事**（3 人收敛） |
| 2.2 | **`session_id` 的 HMAC 派生需确认** | 该设计是实现时定的，不在规格里。若 `conversation_id` 由前端生成且可枚举，攻击者虽算不出他人 session_id，但能让**自己的**多个会话碰撞。是否改为服务端下发 `conversation_id`？ | P2.3.1 之前 |
| 2.3 | **`before_value`/`after_value` 对 CREATE 类操作的填充规则未定** | 创建订单没有"变更前状态"：`before_value` 填 `null` 还是不填？`after_value` 填整行还是变化字段？P2 只有 create 不紧急，`update_order_status` 出现时会更需要 | P2.3.4 之前 |
| 2.4 | **外键决策的三项补偿手段** | §4.0 把它们标为「本决策成立的**前提**」而非建议：种子生成器自检孤儿行为 0 · 只读工具一律 `INNER JOIN` · 写端点插入订单行前校验 `order_no` 存在 | 随 P2.1.7 / P2.2.x / P2.3.4 |

## 3. P2 主体（23 项任务，WBS 已分解）

| 组 | 内容 | 备注 |
|---|---|---|
| P2.1.1–1.8 | 九张业务表 + Alembic 基线 | §4 DDL 已于 2026-09-09 同步到 WBS 决定，**可直接照 §4 实现** |
| P2.2.1–2.3 | fixture 规格 + 生成器 + 生成后断言 | 宪法第八条的落点（固定化名词表）在此 |
| P2.3.1–3.6 | FastAPI 骨架 · 响应包络 · 分页 · 五查询 + 一写入 · OpenAPI · Dify 工具导入探针 | |
| P2.4.1–4.4 | 静态权限模型 · `region` 字段 · 评分细则 · API 文档 | |
| P2.5.1–5.2 | 文档编写约定 · 8 份知识库文档骨架 | |

## 4. P3 及以后

| # | 任务 | 依赖 |
|---|---|---|
| 4.1 | **宪法第二条机械检查**（写路径只走 LangGraph） | 需 P2 子图存在才有可检对象 |
| 4.2 | **宪法第五条机械检查**（不绕过 Gateway） | 同上 |
| 4.3 | **宪法第八条落地**（脱敏） | P2.2.x 的化名词表 |
| 4.4 | **宪法第九条机械检查**（引用可溯源）+ `evidence_ref` 填充 | P3 RAG |
| 4.5 | **宪法第十条机械检查**（不可信输入 / 注入用例 6–8 条） | P3 RAG |
| 4.6 | **`retrieval_ms` 采集点** | P3 RAG。列已建，别忘了填 |
| 4.7 | **`interrupt()` 拓扑拆分 + AST 机械检查** | 宪法一之附则的第 3 条验证方式：扫描节点函数，同时出现 `interrupt(` 与写操作即失败 |

> ⚠️ **2026-09-09 复核更正**：评审报告原写「四条宪法无机械检查」，实为**五条** ——
> 漏算了第十条。当时把 `test_llm_does_not_import_audit_or_db` 误当成它的检查（那是
> 分层检查），把 `extra_body 注入` 误当成提示注入（词同义不同）。第九条同为假阳性：
> `evidence_ref` 只出现在防篡改测试的 `DROP COLUMN` 攻击串里。

## 5. 外部依赖与已接受的风险

| # | 事项 | 状态 |
|---|---|---|
| 5.1 | **D1 · AutoDL 实例 SSH 凭据**（用户侧） | ⏳ 未到位。`AUTODL_SSH_HOST` / `AUTODL_VLLM_API_KEY` 均未设置，**M1/M2 两档完全未验证**。`chat_template_kwargs.enable_thinking` 在 vLLM 上的形状是**查文档所得、非实测** —— M3/M4 的教训是四种形状互不通用。**死线：P2 结束** |
| 5.2 | **仓库可见性** | 当前 **PUBLIC**（2026-09-09 用户明确暂不转私有）。设计文档、QC 语料均已公开 |
| 5.3 | **Dify 以 postgres（超级用户）连库** | 已接受（2026-09-03 决策，宪法附注 A.1）。切换脚本 `scripts/05` 已备好未执行。**在切换前，对外不得无限定地声称「审计日志不可篡改」** |
| 5.4 | **LangGraph 1.2.11 官方未声明支持 Python 3.14** | 实测 import/构图/invoke/interrupt/resume 全通，已固化为回归测试。风险可控但须记录在案 |

---

## 6. 当前可合入状态

| 项 | 状态 |
|---|---|
| 三条出口判据 | ✅ 全 PASS（`make p1-exit` 可复现） |
| CI | ✅ 连续三次绿（2026-09-09 首次转绿前，**从未绿过**） |
| 测试 | 88 项，0 跳过 |
| 工作区 | 干净，本地与远程同步 |
| 阻塞合入的项 | **无** |

上面 §1–§2 的七项都不阻塞合入 —— 它们要么补的是"性质的背书"而非机制本身，
要么必须等 P2 有代码才有可检对象。
